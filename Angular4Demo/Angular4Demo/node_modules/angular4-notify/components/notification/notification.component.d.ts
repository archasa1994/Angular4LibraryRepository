import { EventEmitter } from '@angular/core';
import { Notification } from '../../models';
export declare class NotificationComponent {
    notification: Notification;
    dismissNotify: EventEmitter<{}>;
    dismiss(): void;
}
