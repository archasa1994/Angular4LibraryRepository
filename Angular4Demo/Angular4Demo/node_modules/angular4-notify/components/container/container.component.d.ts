import { OnDestroy, OnInit } from '@angular/core';
import { Subscription } from 'rxjs/Subscription';
import { Notification } from '../../models';
import { NotificationsService } from '../../services/notifications.service';
import { Router } from "@angular/router";
import 'rxjs/add/operator/filter';
export declare class ContainerComponent implements OnInit, OnDestroy {
    protected notificationsService: NotificationsService;
    protected router: Router;
    protected sub: Subscription;
    protected routerSub: Subscription;
    protected notifications: Notification[];
    protected render(notification: any): void;
    protected findSimilar(notification: Notification): Notification;
    constructor(notificationsService: NotificationsService, router: Router);
    ngOnInit(): void;
    ngOnDestroy(): void;
    onDismissNotify(n: Notification): Notification[];
}
